import { makeStyles } from "@material-ui/core/styles";

 export default makeStyles((theme) => ({
   container:{
    width: '100%',
    display: 'flex',
    marginTop: 20,
    justifyContent: 'center',
    gap: 20,
    flexWrap: 'wrap',
    overflowY: 'auto',
    paddingBottom: 30,
   }
 }))